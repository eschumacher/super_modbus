#!/bin/bash
# Pre-commit hook to automatically format code with clang-format
# Formats staged C++ files and re-stages them automatically

# Colors for output
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo "Running clang-format on staged files..."

# Get list of staged C++ files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(cpp|hpp|h)$')

if [ -z "$STAGED_FILES" ]; then
  echo -e "${GREEN}No C++ files to format.${NC}"
  exit 0
fi

FORMATTED_FILES=()

# Format all staged C++ files
for file in $STAGED_FILES; do
  # Skip if file doesn't exist (was deleted)
  if [ ! -f "$file" ]; then
    continue
  fi

  # Check if file needs formatting
  if ! clang-format --dry-run --Werror "$file" >/dev/null 2>&1; then
    # Format the file
    clang-format -i "$file"
    FORMATTED_FILES+=("$file")
    echo -e "${YELLOW}Formatted: $file${NC}"
  fi
done

if [ ${#FORMATTED_FILES[@]} -eq 0 ]; then
  echo -e "${GREEN}✓ All files are already properly formatted${NC}"
  exit 0
fi

# Auto-re-stage formatted files
echo ""
echo -e "${GREEN}Auto-formatted ${#FORMATTED_FILES[@]} file(s). Re-staging...${NC}"
for file in "${FORMATTED_FILES[@]}"; do
  git add "$file"
done
echo -e "${GREEN}✓ Files re-staged. Continuing with commit...${NC}"

exit 0
